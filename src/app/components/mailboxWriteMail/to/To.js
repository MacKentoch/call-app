import React, { PropTypes } from 'react';

const To = ({}) => {
  return (
    <div className="form-group">
      <input
        className="form-control"
        placeholder="à:"
      />
    </div>
  );
};

export default To;
